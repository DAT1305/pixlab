{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the local PixLab desktop shell.",
  "windows": ["main", "pet-overlay"],
  "permissions": [
    "core:default",
    "deep-link:default"
  ]
}
